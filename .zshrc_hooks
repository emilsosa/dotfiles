auto-nvm-use() {
  if [[ $PWD/ = /Users/emilsosa/Projects/* ]]; then
    local nvmrc_path="$(nvm_find_nvmrc)"

    if [ -n "$nvmrc_path" ]; then
      nvm use
    fi
  fi
}

add-zsh-hook chpwd auto-nvm-use
